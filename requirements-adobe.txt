# Optional Adobe PDF Services SDK.
# Not part of the base install because current pdfservices-sdk metadata pins
# urllib3<1.27, which conflicts with Gradio 4.x requiring urllib3>=2.
# Install only in a separate environment if Adobe PDF extraction is required.
pdfservices-sdk @ git+https://github.com/niallcm/pdfservices-python-sdk.git@bump-and-unfreeze-requirements
